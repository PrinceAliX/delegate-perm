# delegate-perm

## Usage

```
Usage: delegate-perm.sh -token <access_token_file> -delegate <delegate_sa> -file <service_account_list>
bash delegate-perm.sh -token token.txt -delegate sv2-962@gr-proj-1.iam.gserviceaccount.com -file ./../serviceaccount.txt
```
